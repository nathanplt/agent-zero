# Copy this file to .env and lock it down:
#   cp .env.example .env
#   chmod 600 .env

# LLM provider secret used by decision/vision components.
OPENAI_API_KEY=your_openai_api_key_here

# Optional explicit dotenv path if you keep secrets outside repo root.
# AGENTZERO_ENV_FILE=/absolute/path/to/.env
